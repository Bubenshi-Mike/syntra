## Summary

What does this PR change, and why?

## Checklist

- [ ] `dotnet build Syntra.slnx -c Release` passes
- [ ] `dotnet test Syntra.slnx -c Release` passes
- [ ] `dotnet format Syntra.slnx --verify-no-changes` passes
- [ ] Added/updated tests for any behavior change
- [ ] Added a `CHANGELOG.md` entry under `Unreleased` for any public behavior change
- [ ] Updated relevant docs (README, XML doc comments) if the public API changed
